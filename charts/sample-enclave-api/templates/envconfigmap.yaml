apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sample-enclave-api.fullname" . }}-app-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sample-enclave-api.labels" . | nindent 4 }}
data:
{{- range $key, $val := .Values.app.env }}
  {{ $key }} : {{ $val | quote }}
{{- end}}