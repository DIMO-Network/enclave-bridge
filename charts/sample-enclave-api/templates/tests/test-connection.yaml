apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-enclave-api.fullname" . }}-test-connection"
  labels:
    {{- include "sample-enclave-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-enclave-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
