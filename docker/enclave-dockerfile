FROM amazonlinux:2

# Install packages
#
RUN amazon-linux-extras install aws-nitro-enclaves-cli -y && \
    yum install procps-ng jq -y && \
    yum clean all && \
    rm -rf /var/cache/yum

COPY docker/run.sh  /
COPY bin/sample-enclave-app.eif /eif/

CMD ["/run.sh", "start"]
